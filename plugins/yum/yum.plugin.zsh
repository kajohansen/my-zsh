## Aliases

alias ys="yum search"                       # search package
alias yp="yum info"                         # show package info
alias yl="yum list"                         # list packages
alias ygl="yum grouplist"                   # list package groups
alias yli="yum list installed"              # print all installed packages
alias ymc="yum makecache"                   # rebuilds the yum package list

alias yu="yum update"                  # upgrate packages
alias yi="yum install"                 # install package
alias ygi="yum groupinstall"           # install package group
alias yr="yum remove"                  # remove package
alias ygr="yum groupremove"            # remove pagage group
alias yrl="yum remove --remove-leaves" # remove package and leaves
alias yc="yum clean all"               # clean cache